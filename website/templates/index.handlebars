<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HexaGo - Welcome</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        html,
        body {
            height: 100%;
            width: 100%;
            margin: 0;
            background: linear-gradient(135deg, rgb(69, 28, 113), rgb(19, 72, 163));
            font-family: 'Arial', sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: auto;
        }

        .welcome-card {
            background-color: #fff;
            color: #333;
            min-height: 65vh;
            padding-top: 4rem;
            margin-bottom: 5%;
            margin-left: 20px;
            margin-right: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 900px;
            overflow-y: auto;
            text-align: center;
        }

        .welcome-card h1 {
            font-size: 3rem;
            font-weight: bold;
            margin-bottom: 1.5rem;
            color: #6a11cb;
        }

        .welcome-card p {
            font-size: 1.2rem;
            margin-bottom: 2rem;
        }

        .btn-primary {
            background-color: #6a11cb;
            border: none;
        }

        .btn-primary:hover {
            background-color: #4d67ff;
        }

        .btn-secondary {
            background-color: #6c757d;
            border: none;
        }

        .btn-secondary:hover {
            background-color: #5a6268;
        }

        .mt-4 {
            margin-top: 2rem !important;
        }
    </style>
</head>

<body>
    <div class="welcome-card">
        <h1>Welcome to HexaGo</h1>
        <p class="lead">Learn and connect with universities around the world.</p>
        <div class="mt-4">
            <a href="/sign-up-university" class="btn btn-primary btn-lg me-3">Sign Up As University</a>
            <a href="/sign-up-student" class="btn btn-primary btn-lg">Sign Up As Student</a>
        </div>
        <div class="mt-4">
            <p class="lead">Already have an account?</p>
            <a href="/login" class="btn btn-secondary btn-lg">Login</a>
        </div>
    </div>
</body>

</html>